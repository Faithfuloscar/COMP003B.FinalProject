@model IEnumerable<COMP003B.FinalProject.Models.FitnessGoal>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GoalType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TargetDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GoalType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TargetDate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FitnessGoalId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FitnessGoalId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FitnessGoalId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
